import SubLayout from "@/layouts/SubLayout";

const Home = () => {
  return (
    // main으로 묶어주기. 마치 react-native의 view같은 존재인가...
    <main>
      <div>
        <p>이벤트 페이지</p>
        <a href="/">홈페이지 가기</a>
      </div>
    </main>
  );
};

export default Home;
